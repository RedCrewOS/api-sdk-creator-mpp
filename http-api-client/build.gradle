plugins {
    id "apisdkcreator.configuration"
}

version "0.1-SNAPSHOT"

dependencies {
    api platform("io.arrow-kt:arrow-stack:${rootProject.ext.arrowVersion}")

    api "io.arrow-kt:arrow-core"
    api "io.arrow-kt:arrow-fx-coroutines"

    testImplementation project(":test")
}
