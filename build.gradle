plugins {
    id "org.jetbrains.dokka" version "1.4.0"
}

allprojects {
    repositories {
        mavenCentral()
        maven { url "https://jitpack.io" }

        // Needed for Dokka
        jcenter()
    }
}

ext {
    arrowVersion = "1.0.1"
    kotlinCoroutinesVersion = "1.6.0"
    kotestVersion = "5.1.0"
}
