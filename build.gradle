plugins {
    id "io.github.gradle-nexus.publish-plugin"
    id "org.jetbrains.dokka"
}

allprojects {
    repositories {
        mavenCentral()

        // Needed for Dokka
        jcenter()
    }
}

ext {
    arrowVersion = "1.0.1"
    kotlinCoroutinesVersion = "1.6.0"
    kotestVersion = "5.1.0"
}
